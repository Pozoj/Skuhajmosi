#advance_search
  %h4 NAPREDNO ISKANJE
  = form_tag recipes_path, :method => 'get' do
    %div
      %label{ :for => "specifics_nr_of_people" } Kaj kuhati za
      = text_field :specifics, :nr_of_people
      oseb?
    %div
      %label{ :for => "specifics_min_price" } Naj bo cena med
      = text_field :specifics, :min_price
      in
      = text_field :specifics, :max_price
      &euro;.
    %div
      %label{ :for => "specifics_min_kcal" } Kcal naj bo med
      = text_field :specifics, :min_kcal
      in
      = text_field :specifics, :max_kcal
    
    = submit_tag "Išči", :class => "submit"
