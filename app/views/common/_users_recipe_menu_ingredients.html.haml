- if @ingredients.present?
  #shopping-list
    %h3 Kaj potrebujem?
    %h4 Sestavine potrebne za jedi v tedenskem jedilniku
    
    %ul
      - @ingredients.each do |ingredient, quantity|
        %li
          = ingredient
          %span
            = quantity_fixer(quantity)
    
    %h3.sum 
      Skupaj 
      %span= number_to_currency(@price)
