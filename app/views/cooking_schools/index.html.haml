%section
  = title "Šola kuhanja"
  
  %h2 Tu se lahko učite kuharskih veščin
  
  - if can? :create, CookingSchool
    %nav#editors-resource-menu
      %ul
        %li= link_to "Dodaj zapis", new_resource_path

  - for post in collection
    %article
      %h1.title= link_to post.title, post
      .content= RedCloth.new(truncate(post.content, :length => 120, :separator => ' ', :omission => '...')).to_html.html_safe
%aside