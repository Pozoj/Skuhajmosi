= title "Recepti"
= params.inspect
#resource-menu
  - if can? :create, Recipe
    = link_to "Dodaj recept", new_resource_path
    
  = link_to "Prenesi pdf", recipes_path("pdf")  

#search
  = form_tag recipes_path, :method => 'get' do
    %p
      = text_field_tag :search, params[:search]
      = submit_tag "Poišči recept", :name => nil

= collection_list_renderer(collection)