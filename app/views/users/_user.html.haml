- collection.group_by(&:user_kind).each do |user_kind, users|
  - if user_kind.present?
    %h3= user_kind
  - else
    %h3 Administratorji
  %ul
    - for user in users
      %li
        = link_to user, user
        = render "common/small_photo_link", :resource => user
  
  