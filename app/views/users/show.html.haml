%section
  = title "Pregled uporabnika"

  - if can? :manage, resource
    %nav#editors-resource-menu
      %ul
        %li= link_to "Uredi uporabnika", edit_resource_path
        %li= link_to "Odstrani uporabnika", resource_path, :method => :delete, :confirm => destroy_confirmation(resource)
        %li= link_to "Dodaj fotografijo", new_user_photo_path(resource)  

  = render "common/photo", :resource => resource

  .field
    .description Ime in priimek
    .value= resource.full_name
  
  .field
    .description Elektronska pošta
    .value= resource.email
  
  .field
    .description Vrsta uporabnika
    .value= resource.user_kind

  - if resource.user_kind and resource.access_rights.any?
    = render "access_rights/access_right", :collection => resource.access_rights

  = link_to_back
%aside