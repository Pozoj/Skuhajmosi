= title resource

#resource-menu
  - if can? :update, resource
    = link_to "Uredi ponudnika", edit_resource_path
  - if can? :destroy, resource
    = link_to "Odstrani ponudnika", resource_path, :method => :delete, :confirm => destroy_confirmation(resource)

#basic-info
  #description-bar
    .field
      .description Naziv ponudnika
      .value= resource

= link_to_back